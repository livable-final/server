package com.livable.server.review.repository;

import com.livable.server.entity.ReviewMenuMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReviewMenuMapRepository extends JpaRepository<ReviewMenuMap, Long> {
}
